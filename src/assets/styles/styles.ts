import {makeStyles} from '@mui/material';


const useStyles = makeStyles(() => ({
  header: {
    position: 'static',
    w: '100vw',
    minHeight: '5vh',
    bgcolor: 'yellowgreen',
    p: '1vh 0',
  },
}));

export default useStyles;
